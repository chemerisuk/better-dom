    // REGISTER API
    // ------------

    window.DOM = DOM;
    
    if (typeof define === "function" && define.amd) {
        define("DOM", function() { return DOM; });
    }
    
})(window, document);
