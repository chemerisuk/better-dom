    
    // REGISTER API
    // ------------

    if (typeof define === "function" && define.amd) {
        define("DOM", function() { return DOM; });
    } else {
        window.DOM = DOM;
    }
    
})(window, document);
