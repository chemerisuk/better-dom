/*global describe it*/
describe("DOMElement", function () {
    "use strict";
    
    describe("DOMElement.on", function () {
	   
        it("may accept only one object parameter");
        
        it("may accept two parameters");
	   
        it("may accept three parameters where ontions is a filter string");
        
        it("may accept three parameters where options is an object");
        
        it("throws Error in other cases");
        
        it("should throw Error if handler is not a function");
        
        it("should accept only simple filter expressions");
        
        it("should call e.preventDefault() when prevent=true");
        
        it("should call e.stopPropagation() when stop=true");
        
        it("should get properties from the event object when args is defined");
        
        it("should get properties in the same order as in event object");
        
        it("should change this value when thisPtr argument is defined");
	
	});

});