/*global describe it*/
describe("DOMElement", function () {
    "use strict";
    
	describe("DOMElement.find", function () {

		it("should find single element by default");

		it("should have custom array implementation if second parameter is true");

		it("should return special null object instance if nothing was found");

		it("should allow only true as the second parameter");

	});
});