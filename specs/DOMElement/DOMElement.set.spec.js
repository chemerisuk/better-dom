/*global describe it*/
describe("DOMElement", function () {
    "use strict";
    
    describe("DOMElement.set", function () {
	   
        it("should set property value when it's available");
        
        it("should set attribute value when property is not found");
        
        it("may accept an object to set several properties/attributes");
        
        it("may accept only string or object as the first parameter");
        
        it("may accept value as a function");
        
        it("should throw an Error if value is not a function or a string");
	
	});

});